# DevPulse Environment Variables
# Copy this file to .env at the project root and fill in your values.
# All variables are optional — DevPulse works without any integrations.

# ── GitHub Integration ──────────────────────────────────────────────
# Personal Access Token with `repo` scope
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here

# Map of project names to GitHub owner/repo (JSON)
# Keys must match the source_app names used by your hooks
GITHUB_REPOS={"my-project": "owner/repo", "another-project": "owner/repo2"}

# ── Vercel Integration ──────────────────────────────────────────────
# API token from https://vercel.com/account/tokens
VERCEL_API_TOKEN=your_vercel_token_here

# Map of project names to Vercel project IDs (JSON)
VERCEL_PROJECTS={"my-project": "prj_xxxxxxxxxxxx"}

# Optional: Vercel team/org ID (required if projects belong to a team)
VERCEL_TEAM_ID=
